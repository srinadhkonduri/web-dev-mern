const parentEle = document.querySelector('#parent')
console.log(parentEle.childNodes); // gives the complete DOM tree structure



const parentEle1 = document.querySelector('#parent')
console.log(parentEle1.children); // gives the elements inside the file and ignores the comments and unnamed text tag's
